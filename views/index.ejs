<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Daily Planner</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        <link rel="stylesheet" href="css/style.css">
    </head>
    <body>
        <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Daily Planner</title>
    </head>
<body>
    <h1>Daily Planner</h1>

    <section class="container">
        <div class="container-item">
            <h3>Add A Task:</h3>

                <form action="/addTodo" method="POST">
                    <input type="text" placeholder="Enter Task ..." name="todoItem">
                    <input type="time" class='todoTime' name="todoTime">
                    <input type="submit" value="Add Task">
                </form>

            <h3>Left to do: <%= left %></h3>
        </div>

        <div class="container-item">
            <ul class="todoItems">
                <% for(let i=0; i < items.length; i++) {%>
                    <li class="item">
                        <% if(items[i].completed === true) {%>
                            <span class='completed'><%= items[i].thing %> <%= items[i].time %></span>
                        <% }else{ %>
                            <span><%= items[i].thing %> <%= items[i].time %></span>
                        <% } %>    
                            <span class='fa fa-trash'></span>
                    </li>
                <% } %>
            </ul> 
        </div>
    </section>
    

    <script src='js/main.js'></script>
</body>
</html>